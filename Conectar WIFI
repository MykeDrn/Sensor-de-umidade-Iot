void conectarWiFi() {
    client.setInsecure();
    WiFi.mode(WIFI_STA);
    WiFi.begin(ssid, password); 
    
    int tentativas = 0; 
    while (WiFi.status() != WL_CONNECTED && tentativas < 20) {
        delay(1000); 
        Serial.print("."); 
        tentativas++; 
    }
    
    if (WiFi.status() != WL_CONNECTED) { 
        ESP.restart();
    }
    else {
        Serial.println("\nConectado!"); 
    }
}
